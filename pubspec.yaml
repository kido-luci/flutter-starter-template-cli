name: flutter_starter_template_cli
description: CLI to scaffold new Flutter projects from flutter-starter-template.
version: 0.6.0
repository: https://github.com/kido-luci/flutter-starter-template-cli

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  args: ^2.5.0
  interact: ^2.2.0
  mason_logger: ^0.3.1
  path: ^1.9.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0

executables:
  fst: fst
